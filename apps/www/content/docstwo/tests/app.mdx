---
title: App Test
description: A simple test automated by CRA.
---

## Start By Adding an React app

<Steps>

### Create an react app with CRA

```bash
npx create-react-app@latest test-app --template typescript
```

### Component

<ComponentPreview name="test-another-demo" />

### Testing component

```js title="app.test.tsx"
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  const linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInTheDocument();
});
```

</Steps>

## Fix the Fix the "not wrapped in act(...)" warning
https://kentcdodds.com/blog/fix-the-not-wrapped-in-act-warning